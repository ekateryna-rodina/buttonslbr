import { Buttonlr } from "../Buttonlr";

export default {
  title: "A customizable button component libary (React)",
  component: Buttonlr,
};

const Template = (args) => <Buttonlr {...args} />;

export const Default = Template.bind({});
